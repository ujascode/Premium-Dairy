import "./globals.css";

// We're using system fonts now, so we don't need Google fonts
// But keeping the structure for potential future use

export const metadata = {
  title: "Dairy Sales Management System",
  description: "Premium dairy farm management solution with analytics and reporting",
  icons: {
    icon: "/favicon.ico",
    shortcut: "/favicon.ico",
  },
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className="h-full antialiased"
    >
      <body className="min-h-full flex flex-col bg-background text-font">
        {children}
      </body>
    </html>
  );
}
